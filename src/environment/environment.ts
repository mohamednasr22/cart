// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,

    /* url: "http://localhost/ParalympiansAPI/api/",
    imgUrl: "http://localhost/Resources/",
    urllocalreport: "http://localhost/",
    Print: "http://localhost/CustomReportEditorDevex/API/Reports/",
    language:"/assets/i18n/"
 
  */
    // localUrl: 'http://localhost/MappingWebApi/api/',

    // server

    // baseUrl: '/MappingWebApi/api/',
    baseUrl: 'http://localhost:3000/',


    // in HI Fw
    // url: "/HI_FW/ParalympiansAPI/api/",
    // imgUrl: "/HI_FW/Resources/",
    // urllocalreport: "/HI_FW/",
    // Print: "/HI_FW/CustomReportEditorDevex/API/Reports/",
    // language: "/HI_FW/paralympians-ui/assets/i18n/"


};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
  // import 'zone.js/plugins/zone-error';  // Included with Angular CLI.